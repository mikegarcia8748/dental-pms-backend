product: jvm/app

dependencies:
  - $ktor.serialization.kotlinx.json
  - $ktor.server.auth
  - $ktor.server.auth.jwt
  - $ktor.server.callLogging
  - $ktor.server.contentNegotiation
  - $ktor.server.core
  - $ktor.server.metrics.micrometer
  - $ktor.server.netty
  - $libs.bcrypt
  - $libs.exposed.core
  - $libs.exposed.jdbc
  - $libs.exposed.javaTime
  - $libs.flyway.core
  - $libs.flyway.postgresql
  - $libs.hikaricp
  - $libs.koin.ktor
  - $libs.koin.loggerSlf4j
  - $libs.ktor.openapi
  - $libs.ktor.swaggerUi
  - $libs.schemakenerator.core
  - $libs.schemakenerator.swagger
  - $libs.logback.classic
  - $libs.micrometer.registryPrometheus
  - $libs.postgresql

test-dependencies:
  - $ktor.client.contentNegotiation
  - $ktor.server.testHost
  - $libs.kotest.runnerJunit5
  - $libs.kotest.assertionsCore
  - $libs.mockk

settings:
  ktor: enabled
  kotlin:
    serialization: enabled
  junit: junit-5
