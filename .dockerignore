# Keep the build context small and secrets out of the image
.git
.gitignore
.idea
*.iml
build/
out/
.amper/
.kotlin/
docs/
*.log

# Secrets: the container gets configuration through environment variables
.env
.env.*
!.env.example

# The wrapper's Windows twin is not used inside the Linux build stage
kotlin.bat
